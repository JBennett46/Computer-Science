#include<iostream>
using namespace std;
int main() {
    int expYear;
    cout << "\nEnter your DL expiration year: ";
    cin >> expYear;
    string(s) = (expYear > 2024) ? "Not expired.\n" : "Expired.\n";
    cout << "\n" << s << "\n\n";
}